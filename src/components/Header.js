import { Button } from "../styles/Button";
const HeaderComponent = () => {
  return (
    <header>
      <Button inputColorVariant="#2980b9" inputColor="#3498db" color="#FFFFFF">
        Generate new color
      </Button>
    </header>
  );
};

export default HeaderComponent;
